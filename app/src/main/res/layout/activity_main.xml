<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/search_vertical_height"
        android:orientation="horizontal">
        <ImageButton
            android:id="@+id/barCodeImageButton"
            android:layout_width="@dimen/search_vertical_width"
            android:layout_height="match_parent"
            android:background="@drawable/ic_barcode"/>

        <EditText
            android:id="@+id/searchItemEditText"
            android:inputType="text"
            android:layout_toRightOf="@+id/barCodeImageButton"
            android:layout_toLeftOf="@id/searchItemImageButton"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <ImageButton
            android:id="@+id/searchItemImageButton"
            android:layout_width="@dimen/search_vertical_width"
            android:layout_alignParentRight="true"
            android:layout_height="wrap_content"
            android:background="@drawable/ic_search"/>
    </RelativeLayout>
    <ImageView
        android:background="@color/colorPrimary"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="検索履歴"/>
</LinearLayout>